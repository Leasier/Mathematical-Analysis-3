- 注：如无特殊说明，下面默认采取 $2$-范数。

### 体积与可求体积

#### 定义：长方体及其体积

设 $\forall 1 \leq i \leq n, a_i < b_i$，称 $\mathbb{R}^n$ 中的集合

$$
A = \prod_{i = 1}^n [a_i, b_i] = \left\{ \mathbf{x} = (x_1, \cdots, x_n) \in \mathbb{R}^n \mid \forall 1 \leq i \leq n, a_i \leq x_i \leq b_i \right\}
$$

为一个 $n$ 维长方体，其体积定义为

$$
V(A) \triangleq \prod_{i = 1}^n (b_i - a_i)
$$

其直径即最远点对间的距离为

$$
\text{diam}(A) = \sqrt{\displaystyle\sum_{i = 1}^n (b_i - a_i)^2}
$$

#### 定义：简单集

称有限个 $n$ 维长方体之并组成的集合为简单集。

#### 定义：恰当覆盖

设 $E \subset \mathbb{R}^n$ 有界，称 $\mathbb{R}^n$ 中的一族长方体 $\{A_j\}_{j = 1}^k \ (k \in \mathbb{N})$ 为 $E$ 的一个恰当覆盖，若：

- (1) $\forall 1 \leq j_1, j_2 \leq k, j_1 \neq j_2 \Rightarrow \mathring{A}_{j_1} \cap \mathring{A}_{j_2} = \varnothing$；
- (2) $E \subset \displaystyle\bigcup_{j = 1}^k A_j$；
- (3) $\forall 1 \leq j \leq k, A_j \cap E \neq \varnothing$。

此时记 $\mathcal{A}_E = \{A_j\}_{j = 1}^k$。显见简单集必有恰当覆盖，使得 $E = \displaystyle\bigcup_{j = 1}^k A_j$。

#### 定义：内和与外和

设 $E \subset \mathbb{R}^n$ 有界，对 $E$ 的一个恰当覆盖 $\mathcal{A}_E = \{A_j\}_{j = 1}^k$，定义其内和与外和为

$$
m(\mathcal{A}_E) = \sum_{A_j \subset E} V(A_j), \quad M(\mathcal{A}_E) = \sum_{j = 1}^k V(A_j),
$$

显见 $m(\mathcal{A}_E) \leq M(\mathcal{A}_E)$。

#### 定义：内体积与外体积

设 $E \subset \mathbb{R}^n$ 有界，则恰当覆盖存在，定义其内体积与外体积为

$$
m(E) = \sup_{\mathcal{A}_E} m(\mathcal{A}_E), \quad M(E) = \inf_{\mathcal{A}_E} M(\mathcal{A}_E)
$$

显见 $m(E) \leq M(E)$。

#### 定义：体积

设 $E \subset \mathbb{R}^n$ 有界，当 $m(E) = M(E)$，称 $E$ 可求体积，并定义其体积为

$$
V(E) = m(E) = M(E)
$$

易得以下性质：

- (1) 设 $E \subset \mathbb{R}^n$ 有界，则 $V(E) = 0$ 当且仅当 $M(E) = 0$。
- (2) 并且对于一族内部互不相交的长方体 $\{A_j\}_{j = 1}^k$ 生成的 $S = \displaystyle\bigcup_{j = 1}^k A_j$，有

$$
V(S) = \sum_{j = 1}^k V(A_j)
$$

- (3) 对于简单集 $E_1, E_2 \subset \mathbb{R}^n$，次可加性成立：$V(E_1 \cup E_2) \leq V(E_1) + V(E_2)$。
- (4) 设 $E \subset \mathbb{R}^n$ 有界，则 $V(E) = 0$ 当且仅当 $\forall \epsilon > 0$，$\exists$ 简单集 $S$，$\text{s.t. } E \subset S, V(S) < \epsilon$。

[例 1] 特别地考虑 $n = 1$ 的情形，设 $E = \mathbb{Q} \cap [0, 1] \subset \mathbb{R}$：

- 一方面，由 $E$ 无内点可见不存在包含于 $E$ 的非空闭区间，故 $m(E) = 0$。
- 另一方面，由 $E$ 在 $[0, 1]$ 中稠密可见覆盖 $E$ 的有限个闭区间之并必包含 $[0, 1]$，故 $M(\mathcal{A}_E) \geq 1$；而 $\{[0, 1]\}$ 即取得 $1$，故 $M(E) = 1$。

综上，$m(E) = 0 < 1 = M(E)$，故 $E$ 不可求体积。

#### 定义：模

设 $E \subset \mathbb{R}^n$ 有界，$\mathcal{A}_E = \{A_j\}_{j = 1}^k$ 为其恰当覆盖，在 $k > 0$ 时定义其模为

$$
\|\mathcal{A}_E\| = \max_{j = 1}^k \text{diam}(A_j)
$$

特别地，当 $k = 0$ 即空覆盖的情形，定义 $\|\mathcal{A}_E\| = 0$。

易见若 $\mathcal{A}_E'$ 为其细化，则 $\|\mathcal{A}'_E\| \leq \|\mathcal{A}_E\|$。

#### 命题：类似 Riemann 积分的体积计算式

内容：设 $E \subset \mathbb{R}^n$ 有界，则：

- (1) $m(E) = \displaystyle\lim_{\|\mathcal{A}_E\| \to 0} m(\mathcal{A}_E)$。
- (2) $M(E) = \displaystyle\lim_{\|\mathcal{A}_E\| \to 0} M(\mathcal{A}_E)$。

证明：

- (1) $\forall \epsilon > 0$，$\exists \mathcal{A}_E = \{A_j\}_{j = 1}^k$ 为 $E$ 的恰当覆盖，使得 $m(\mathcal{A}_E) > m(E) - \dfrac{\epsilon}{2}$。
- 令 $S = \displaystyle\bigcup_{A_j \subset E} A_j$，则 $S \subset E, V(S) = m(\mathcal{A}_E)$。
- 取充分小的 $\delta > 0$，构造一族内缩的长方体 $\{\tilde{A}_j\}_{A_j \subset E}$，使得 $\tilde{A}_j \subset \mathring{A}_j, \text{dis}(\tilde{A}_j, \partial A_j) \geq \delta$，且

$$
\sum_{A_j \subset E} (V(A_j) - V(\tilde{A}_j)) < \frac{\epsilon}{2}
$$

- 任给 $E$ 的恰当覆盖 $\mathcal{A}'_E = \{A'_{j'}\}_{j' = 1}^{k'}$ 使得 $\|\mathcal{A}'_E\| < \delta$，令 $\tilde{S} = \displaystyle\bigcup_{A_j \subset E} \tilde{A}_j \subset E \subset \bigcup_{j' = 1}^{k'} A'_{j'}$。
- 若 $x \in \tilde{S}$ 则 $\exists 1 \leq j \leq k, \text{s.t. } x \in \tilde{A}_j, A_j \subset E$ 并且 $\exists 1 \leq j' \leq k', \text{s.t. } x \in A'_{j'}$，由 $\text{diam}(A'_{j'}) < \delta$ 及 $\{\tilde{A}_j\}_{j = 1}^k$ 的构造可见 $A'_{j'} \subset A_j \subset E$。
- 这就是说当 $A'_{j'} \cap \tilde{S} \neq \varnothing$，必有 $A'_{j'} \subset E$，故

$$
m(\mathcal{A}'_E) \geq \sum_{A'_{j'} \cap \tilde{S} \neq \varnothing} V(A'_{j'}) \geq V(\tilde{S}) = V(S) - \sum_{A_j \subset E} (V(A_j) - V(\tilde{A}_j)) > m(E) - \epsilon
$$

- 即 $|m(\mathcal{A}'_E) - m(E)| < \epsilon$，明所欲证。
- (2) $\forall \epsilon > 0$，$\exists \mathcal{A}_E = \{A_j\}_{j = 1}^k$ 为 $E$ 的恰当覆盖，使得 $M(\mathcal{A}_E) < M(E) + \dfrac{\epsilon}{2}$。
- 令 $S = \displaystyle\bigcup_{j = 1}^k A_j$，则 $E \subset S, V(S) = M(\mathcal{A}_E)$。
- 取充分小的 $\delta > 0$，构造一族外扩的长方体 $\{\tilde{A}_j\}_{j = 1}^k$，使得 $A_j \subset \mathring{\tilde{A}}_j, \text{dis}(A_j, \partial \tilde{A}_j) \geq \delta$，且

$$
\sum_{j = 1}^k (V(\tilde{A}_j) - V(A_j)) < \frac{\epsilon}{2}
$$

- 任给 $E$ 的恰当覆盖 $\mathcal{A}'_E = \{A'_{j'}\}_{j' = 1}^{k'}$ 使得 $\|\mathcal{A}'_E\| < \delta$，令 $E \subset \tilde{S} = \displaystyle\bigcup_{j = 1}^k \tilde{A}_j$。
- 若 $x \in A'_{j'} \cap E$ 则 $x \in \tilde{S}$，由 $\text{diam}(A'_{j'}) < \delta$ 及 $\{\tilde{A}_j\}_{j = 1}^k$ 的构造可见 $A'_{j'} \subset \tilde{A}_j \subset \tilde{S}$。
- 而定义要求了 $A'_{j'} \cap E \neq \varnothing$，故

$$
M(\mathcal{A}'_E) = \sum_{j' = 1}^{k'} V(A'_{j'}) \leq V(\tilde{S}) \leq V(S) + \sum_{A_j \subset E} (V(\tilde{A}_j) - V(A_j)) < M(E) + \epsilon
$$

- 即 $|M(\mathcal{A}'_E) - M(E)| < \epsilon$，明所欲证。

#### 引理：与内外都有交集的道路连通集与边界有交

内容：设 $D \subset \mathbb{R}^n$ 道路连通，$E \subset \mathbb{R}^n$，若 $D \cap E \neq \varnothing, D \not\subset E$，则 $D \cap \partial E \neq \varnothing$。

证明：

- 任取 $\mathbf{x} \in D \cap E, \mathbf{y} \in D \backslash E$，由 $D$ 道路连通可知存在连续映射 $\boldsymbol{\gamma} : [0, 1] \to D$，使得 $\boldsymbol{\gamma}(0) = \mathbf{x}, \boldsymbol{\gamma}(1) = \mathbf{y}$。
- 令 $S = \{t \in [0, 1] \mid \boldsymbol{\gamma}(t) \in E\}$，由 $0 \in S, 1 \not\in S$ 可知 $S$ 非空且有上界，令 $t_0 = \sup S \in [0, 1], \mathbf{z} = \boldsymbol{\gamma}(t_0)$。
- 只需证明 $\mathbf{z} \in \partial E$，分类讨论：
- - (i) 若 $\mathbf{z} \in E$，则 $t_0 < 1$。$\forall \epsilon > 0$，由连续性可知 $\exists \delta > 0, \text{s.t. } \forall t \in U(t_0, \delta) \cap [0, 1], \|\boldsymbol{\gamma}(t) - \mathbf{z}\| < \epsilon$，其中 $t > t_0$ 部分均满足 $\boldsymbol{\gamma}(t) \not\in E$，故 $\mathbf{z} \in \partial E$。
- - (ii) 若 $\mathbf{z} \not\in E$，则 $t_0 > 0$。$\forall \epsilon > 0$，同样由连续性可知 $\exists \delta > 0, \text{s.t. } \forall t \in U(t_0, \delta) \cap [0, 1], \|\boldsymbol{\gamma}(t) - \mathbf{z}\| < \epsilon$，再由确界性质可知 $\exists t \in U^-(t_0, \delta) \cap [0, 1], \text{s.t. } \boldsymbol{\gamma}(t) \in E$，此时 $\|\boldsymbol{\gamma}(t) - \mathbf{z}\| < \epsilon$，故 $\mathbf{z} \in \partial E$。
- 总之有 $\mathbf{z} \in D \cap \partial E \neq \varnothing$。

#### 定理：可求体积的充要条件

内容：设 $E \subset \mathbb{R}^n$ 有界，则 $E$ 可求体积当且仅当 $V(\partial E) = 0$。

证明：

- $\Rightarrow$：$\forall \epsilon > 0$，一方面 $\exists \delta_1 > 0$，$\text{s.t. }$ 任给 $\|\mathcal{A}_E\| < \delta_1$，都有 $m(\mathcal{A}_E) > m(E) - \dfrac{\epsilon}{4}$；另一方面 $\exists \delta_2 > 0$，$\text{s.t. }$ 任给 $\|\mathcal{A}_E\| < \delta_2$，都有 $M(\mathcal{A}_E) < M(E) + \dfrac{\epsilon}{4}$。
- 因此任取恰当覆盖 $\mathcal{A}_E$ 使得 $\|\mathcal{A}_E\| < \min(\delta_1, \delta_2)$，就有

$$
m(E) - \frac{\epsilon}{4} < m(\mathcal{A}_E) \leq M(\mathcal{A}_E) < M(E) + \frac{\epsilon}{4}
$$

- 首先由 $E \subset \displaystyle\bigcup_{j = 1}^k A_j$ 可见 $\bar{E} \subset \displaystyle\overline{\bigcup_{j = 1}^k A_j} = \bigcup_{j = 1}^k A_j$。
- 通过逐坐标内缩得到一族长方体 $\{\tilde{A}_j\}_{j = 1}^k$，使得 $\tilde{A}_j \subset \mathring{A}_j$，且

$$
\sum_{j = 1}^k (V(A_j) - V(\tilde{A}_j)) < \frac{\epsilon}{2}
$$

- 易见每个 $A_j \backslash \tilde{A}_j$ 均为简单集，且可表示为 $3^n - 1$ 个内部互不相交的长方体的并（具体而言，按 $A_j, \tilde{A}_j$ 的坐标端点切分）。
- 令 $\{\tilde{A}_j \mid A_j \not\subset E, \tilde{A}_j \cap \partial E \neq \varnothing\}$ 和这 $(3^n - 1) k$ 个小长方体中与 $\partial E$ 有交者构成 $\mathcal{A}_{\partial E}$。
- 任给 $\mathbf{x} \in \partial E$，则 $\forall r > 0, B_r(\mathbf{x}) \not\subset E$，并可见 $\exists 1 \leq j \leq k, \text{s.t. } \mathbf{x} \in A_j$，分类讨论：
- - (i) 若 $\mathbf{x} \in \tilde{A}_j$，取充分小的 $r$ 即见 $B_r(\mathbf{x}) \subset A_j$，于是 $A_j \not\subset E$，这就是说 $\mathbf{x}$ 被内缩立方体覆盖。
- - (ii) 若 $\mathbf{x} \not\in \tilde{A}_j$，则其被小长方体覆盖。
- 总之 $\mathcal{A}_{\partial E}$ 确为 $\partial E$ 的恰当覆盖，故

$$
\begin{aligned}
M(\partial E) &\leq M(\mathcal{A}_{\partial E}) \\
&\leq \sum_{A_j \not\subset E} V(\tilde{A}_j) + \sum_{j = 1}^k (V(A_j) - V(\tilde{A}_j)) \\
&< \sum_{A_j \not\subset E} V(A_j) + \frac{\epsilon}{2} \\
&= (M(\mathcal{A}_E) - m(\mathcal{A}_E)) + \frac{\epsilon}{2} \\
&< \epsilon
\end{aligned}
$$

- 由 $\epsilon$ 的任意性即得 $M(\partial E) = 0$，故 $V(\partial E) = 0$。
- $\Leftarrow$：$\forall \epsilon > 0$，$\exists$ 简单集 $S$，$\text{s.t. } \partial E \subset S, V(S) < \dfrac{\epsilon}{2}$。取 $S$ 的恰当覆盖 $\mathcal{A}_S = \{A_j\}_{j = 1}^k$。
- 通过逐坐标外扩得到一族长方体 $\{\tilde{A}_j\}_{j = 1}^k$，使得 $A_j \subset \mathring{\tilde{A}}_j$，且

$$
\sum_{j = 1}^k (V(\tilde{A}_j) - V(A_j)) < \frac{\epsilon}{2}
$$

- 令 $\tilde{S} = \displaystyle\bigcup_{j = 1}^k \tilde{A}_j$，则

$$
\exists \delta > 0, \text{s.t. } \forall \mathbf{x} \in S, B_{\delta}(\mathbf{x}) \subset \tilde{S}
$$

- 由 $E$ 有界可取长方体 $I \supset E$，将其切分为有限个内部互不相交的长方体之并，保留其中与 $E$ 有交者得到 $\mathcal{A}_E = \{A'_{j'}\}_{j' = 1}^{k'}$，使得 $\forall 1 \leq j' \leq k', \text{diam}(A'_{j'}) < \delta$。
- 当 $A'_{j'} \not\subset E$，由引理可知 $A'_{j'} \cap \partial E \neq \varnothing$，任取 $\mathbf{x} \in A'_{j'} \cap \partial E \subset S$，则 $A'_{j'} \subset B_{\delta}(\mathbf{x}) \subset \tilde{S}$。
- 故有

$$
\begin{aligned}
M(\mathcal{A}_E) - m(\mathcal{A}_E) &= \sum_{A'_{j'} \not\subset E} V(A'_{j'}) \\
&\leq \sum_{A'_{j'} \cap \partial E \neq \varnothing} V(A'_{j'}) \\
&\leq V(\tilde{S}) \\
&\leq V(S) + \sum_{j = 1}^k (V(\tilde{A}_j) - V(A_j)) \\
&< \epsilon
\end{aligned}
$$

- 故有 $M(E) - m(E) < \epsilon$，由 $\epsilon$ 的任意性得到 $m(E) = M(E)$，即 $E$ 可求体积。

#### 命题：体积的可加性

内容：设 $E, F \subset \mathbb{R}^n$ 可求体积，且 $E \cap F = \varnothing$，则 $E \cup F$ 可求体积，且 $V(E \cup F) = V(E) + V(F)$。

证明：

- $\forall \epsilon > 0$，一方面 $\exists \mathcal{A}_E = \{A^{(E)}_{j_1}\}_{j_1 = 1}^{k_1}$ 为 $E$ 的恰当覆盖，使得 $m(\mathcal{A}_E) > V(E) - \dfrac{\epsilon}{2}$；另一方面 $\exists \mathcal{A}_F = \{A^{(F)}_{j_2}\}_{j_2 = 1}^{k_2}$ 为 $F$ 的恰当覆盖，使得 $m(\mathcal{A}_F) > V(F) - \dfrac{\epsilon}{2}$。
- 令 $P = \displaystyle \left( \bigcup_{A^{(E)}_{j_1} \subset E} A^{(E)}_{j_1} \right) \cup \left( \bigcup_{A^{(F)}_{j_2} \subset F} A^{(F)}_{j_2} \right), Q = \displaystyle \left( \left( \bigcup_{j_1 = 1}^{k_1} A^{(E)}_{j_1} \right) \cup \left( \bigcup_{j_2 = 1}^{k_2} A^{(F)}_{j_2} \right) \right) \backslash P$，构造 $E \cup F$ 之恰当覆盖 $\mathcal{A}_{E \cup F}$ 如下：
- - (i) 包含 $P$ 中各长方体；
- - (ii) 将 $Q$ 切分为若干内部互不相交的长方体，加入这些长方体中与 $E \cup F$ 有交者。
- 则有

$$
m(E \cup F) \geq m(\mathcal{A}_{E \cup F}) \geq V(P) = m(\mathcal{A}_E) + m(\mathcal{A}_F) > V(E) + V(F) - \epsilon
$$

- 由 $\epsilon$ 的任意性即得 $m(E \cup F) \geq V(E) + V(F)$。
- 而若取 $M(\mathcal{A}_E) < V(E) + \dfrac{\epsilon}{2}, M(\mathcal{A}_F) < V(F) + \dfrac{\epsilon}{2}$，同上构造 $\mathcal{A}_{E \cup F}$，则有

$$
M(E \cup F) \leq M(\mathcal{A}_{E \cup F}) \leq V \left( \left( \bigcup_{j_1 = 1}^{k_1} A^{(E)}_{j_1} \right) \cup \left( \bigcup_{j_2 = 1}^{k_2} A^{(F)}_{j_2} \right) \right) \leq M(\mathcal{A}_E) + M(\mathcal{A}_F) < V(E) + V(F) + \epsilon
$$

- 继而类似地有 $M(E \cup F) \leq V(E) + V(F)$。最后由 $m(E \cup F) \leq M(E \cup F)$ 即证得

$$
V(E \cup F) = m(E \cup F) = M(E \cup F) = V(E) + V(F)
$$

#### 命题：零体积的封闭性

内容：

- (1) 设 $A \subset \mathbb{R}^n$ 的体积为 $0$，则 $B \subset A$ 的体积均为 $0$。
- (2) 设 $A, B \subset \mathbb{R}^n$ 的体积均为 $0$，则 $A \cup B$ 的体积为 $0$。

证明：

- (1) $A$ 可被体积任意小的简单集 $S$ 覆盖，则 $B \subset A \subset S$，故 $B$ 的体积为 $0$。
- (2) $A, B$ 可分别被体积任意小的简单集 $S, T$ 覆盖，则 $A \cup B \subset S \cup T$，而 $S \cup T$ 均为简单集，故 $A \cup B$ 的体积为 $0$。

#### 推论：可求体积的封闭性

内容：设 $A, B \subset \mathbb{R}^n$ 均可求体积，则 $A \cup B, A \cap B, A \backslash B$ 均可求体积。

证明：

- 先证 $\partial (A \cup B) \subset \partial A \cup \partial B, \partial (A \cap B) \subset \partial A \cup \partial B, \partial (A \backslash B) \subset \partial A \cup \partial B$：
- - (i)

$$
\begin{aligned}
\partial (A \cup B) &= \overline{A \cup B} \cap \overline{\mathbb{R}^n \backslash (A \cup B)} \\
&= \overline{A \cup B} \cap \overline{(\mathbb{R}^n \backslash A) \cap (\mathbb{R}^n \backslash B)} \\
&\subset (\bar{A} \cup \bar{B}) \cap (\overline{\mathbb{R}^n \backslash A} \cap \overline{\mathbb{R}^n \backslash B}) \\
&= (\bar{A} \cap \overline{\mathbb{R}^n \backslash A} \cap \overline{\mathbb{R}^n \backslash B}) \cup (\bar{B} \cap \overline{\mathbb{R}^n \backslash A} \cap \overline{\mathbb{R}^n \backslash B}) \\
&\subset (\bar{A} \cap \overline{\mathbb{R}^n \backslash A}) \cup (\bar{B} \cap \overline{\mathbb{R}^n \backslash B}) \\
&= \partial A \cup \partial B
\end{aligned}
$$

- - (ii)

$$
\begin{aligned}
\partial (A \cap B) &= \overline{A \cap B} \cap \overline{\mathbb{R}^n \backslash (A \cap B)} \\
&= \overline{A \cap B} \cap \overline{(\mathbb{R}^n \backslash A) \cup (\mathbb{R}^n \backslash B)} \\
&\subset (\bar{A} \cap \bar{B}) \cap (\overline{\mathbb{R}^n \backslash A} \cup \overline{\mathbb{R}^n \backslash B}) \\
&= (\bar{A} \cap \bar{B} \cap \overline{\mathbb{R}^n \backslash A}) \cup (\bar{A} \cap \bar{B} \cap \overline{\mathbb{R}^n \backslash B}) \\
&\subset (\bar{A} \cup \overline{\mathbb{R}^n \backslash A}) \cup (\bar{B} \cup \overline{\mathbb{R}^n \backslash B}) \\
&= \partial A \cup \partial B
\end{aligned}
$$

- - (iii) $\partial (A \backslash B) = \partial (A \cap B^c)$，而 $\partial B^c = \partial B$，故由 (ii) 即得。
- 由 $A, B$ 可求体积可知 $\partial A, \partial B$ 的体积均为 $0$，由上一命题可知 $\partial (A \cup B), \partial (A \cap B), \partial (A \backslash B)$ 的体积均为 $0$，故 $A \cup B, A \cap B, A \backslash B$ 均可求体积。

#### 推论：可求体积者的闭包仍可求体积

内容：设 $E \subset \mathbb{R}^n$ 可求体积，则 $\bar{E}$ 可求体积，且 $V(\bar{E}) = V(E)$。

证明：由 $E$ 可求体积可知 $V(\partial E) = 0$，令 $F = \partial E \backslash E$，则 $V(F) = 0$，故 $\bar{E} = E \cup \partial E = E \sqcup F$ 也可求体积，并有 $V(\bar{E}) = V(E) + V(F) = V(E)$。

#### 推论：体积的次可加性

内容：设 $E, F \subset \mathbb{R}^n$ 可求体积，则 $V(E \cup F) \leq V(E) + V(F)$。

证明：$\forall \epsilon > 0$，$E, F$ 可分别被简单集 $S, T$ 覆盖，使得 $V(S) < V(E) + \dfrac{\epsilon}{2}, V(T) < V(F) + \dfrac{\epsilon}{2}$，则 $E \cup F \subset S \cup T$，而 $S \cup T$ 均为简单集，故由简单集体积的次可加性可知 $M(E \cup F) \leq V(S) + V(T) < V(E) + V(F) + \epsilon$，最后由 $\epsilon$ 的任意性即得 $V(E \cup F) = M(E \cup F) \leq V(E) + V(F)$。

### 多重积分

#### 定义：分割

设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$f$ 在 $D$ 上有定义，称 $P = \{\Delta D_j\}_{j = 1}^k$ 为 $D$ 的一个分割，若：

- (1) 每个 $\Delta D_i$ 为可求体积的闭集；
- (2) $\forall 1 \leq j_1, j_2 \leq k, j_1 \neq j_2 \Rightarrow V(\Delta D_{j_1} \cap \Delta D_{j_2}) = 0$；
- (3) $D = \displaystyle\bigcup_{j = 1}^k \Delta D_j$。

令 $\Delta V_j \triangleq V(\Delta D_j), d_j \triangleq \text{diam}(\Delta D_j)$，定义

$$
\|P\| / \lambda(P) = \max_{j = 1}^k d_j
$$

称分割 $P$ 的模。

#### 定义：$n$ 重积分

设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$P = \{\Delta D_j\}_{j = 1}^k$ 为 $D$ 的一个分割，对每个 $1 \leq j \leq k$ 取 $\boldsymbol{\xi}_j \in \Delta D_j$，称 $\boldsymbol{\xi} = \{\boldsymbol{\xi}_j\}_{j = 1}^k$ 为分割 $P$ 对应的一个取点法，并定义其 Riemann 和为

$$
S_f(P, \boldsymbol{\xi}) \triangleq \sum_{j = 1}^k f(\boldsymbol{\xi}_j) \Delta V_j
$$

若存在 $I \in \mathbb{R}$，使得 $\forall \epsilon > 0$，$\exists \delta > 0$，$\text{s.t. }$ 只要 $\|P\| < \delta$，对于任意相应的取点法 $\boldsymbol{\xi}$ 都有 $|S_f(P, \boldsymbol{\xi}) - I| < \epsilon$，则称 $f$ 在 $D$ 上可积，记作 $f \in R(D)$，并称 $I$ 为 $f$ 在 $D$ 上的 $n$ 重积分，记作

$$
I = \underbrace{\int \cdots \int}_n f(x_1, \cdots, x_n) \text{d} x_1 \cdots \text{d} x_n = \int_D f(\mathbf{x}) \text{d} \mathbf{x} = \int_D f(\mathbf{x}) \text{d} \mathbf{v}
$$

特别地，取 $f \equiv 1$，得到 $V(D) = \displaystyle\int_D \text{d} \mathbf{x} = \int_D \text{d} \mathbf{v}$。

#### 定理：可积蕴涵有界

内容：设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，若 $f \in R(D)$，则 $f$ 在 $D$ 上有界。

#### 定义：Darboux 上下和、振幅

设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$P = \{\Delta D_j\}_{j = 1}^k$ 为 $D$ 的一个分割，$f$ 在 $D$ 上有界，令

$$
M_j \triangleq \sup_{\Delta D_j} f, \quad m_j \triangleq \inf_{\Delta D_j} f, \quad \omega_j \triangleq M_j - m_j
$$

定义 $f$ 关于 $P$ 的 Darboux 上、下和

$$
\bar{S}_f(P) = \sum_{j = 1}^k M_j \Delta V_j, \quad \underline{S}_f(P) = \sum_{j = 1}^k m_j \Delta V_j
$$

再定义 $f$ 关于 $P$ 的振幅

$$
\Omega_f(P) = \bar{S}_f(P) - \underline{S}_f(P) = \sum_{j = 1}^k \omega_j \Delta V_j
$$

#### 定义：细化

设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$P = \{\Delta D_j\}_{j = 1}^k, P' = \{\Delta D'_{j'}\}_{j' = 1}^{k'}$ 为 $D$ 的两个分割，称 $P'$ 为 $P$ 的细化，若 $\forall 1 \leq j' \leq k', \exists 1 \leq j \leq k, \text{s.t. } \Delta D'_{j'} \subset \Delta D_j$。

#### 命题：细化的单调性

内容：若 $P'$ 为 $P$ 的细化，则 $\underline{S}_f(P) \leq \underline{S}_f(P') \leq \bar{S}_f(P') \leq \bar{S}_f(P)$。

#### 命题：分割的公共细化

内容：对 $D$ 的任意两个分割 $P, P'$，存在 $D$ 的分割 $Q$，使之同时为 $P, P'$ 的细化。

证明：设 $P = \{\Delta D_j\}_{j = 1}^k, P' = \{\Delta D'_{j'}\}_{j' = 1}^{k'}$，取所有 $\Delta D_j \cap \Delta D'_{j'} \ (1 \leq j \leq k, 1 \leq j' \leq k')$ 中非空者即构成 $P, P'$ 的一个公共细化 $Q$。

#### 定义：Darboux 上下积分

设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$f$ 在 $D$ 上有界，分别定义 $f$ 在 $D$ 上的（Darboux）上、下积分

$$
\bar{I} = \inf_P \bar{S}_f(P), \quad \underline{I} = \sup_{P} \underline{S}_f(P)
$$

#### Darboux 定理

内容：设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$f$ 在 $D$ 上有界，则：

- (1) $\displaystyle\lim_{\|P\| \to 0} \bar{S}_f(P) = \bar{I}$。
- (2) $\displaystyle\lim_{\|P\| \to 0} \underline{S}_f(P) = \underline{I}$。

#### 定理：可积的等价命题

内容：设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$f$ 在 $D$ 上有界，则下列命题等价：

- (1) $\forall \epsilon > 0, \exists P, \text{s.t. } \Omega_f(P) < \epsilon$。
- (2) $\underline{I} = \overline{I}$。
- (3) $\displaystyle\lim_{\|P\| \to 0} \Omega_f(P) = 0$。
- (4) $f \in R(D)$。

#### 推论：连续函数可积

内容：设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，若 $f \in C(D)$，则 $f \in R(D)$。

#### 定义：零测集

设 $E \subset \mathbb{R}^n$，若 $\forall \epsilon > 0$，$\exists$ 至多可数个覆盖 $E$ 的长方体 $\{A_j\}_{j \geq 1}$，$\text{s.t. } \displaystyle\sum_{j \geq 1} V(A_j) \leq \epsilon$，则称 $E$ 为零测集。

易见体积为零的集合也是零测集。

#### Lebesgue 定理

内容：设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$f$ 在 $D$ 上有界，则 $f \in R(D)$ 当且仅当 $D_{\text{dc}}(f)$ 为零测集，其中 $D_{\text{dc}}(f)$ 表示 $f$ 在 $D$ 上的间断点集。

#### 定理：复合函数的可积性

内容：设 $D \subset \mathbb{R}^n$ 为可求体积的有界闭区域，$f \in R(D)$，$m = \displaystyle\inf_D f, M = \sup_D f$，若 $\varphi$ 在 $[m, M]$ 上连续，则 $\varphi \circ f \in R(D)$。

#### 命题：重积分的若干基本性质

内容：设 $D \subset \mathbb{R}^n$ 为有限个内部互不相交的可求体积的有界闭区域之并，$f, g \in R(D)$，则：

- (1) 线性性：$\forall \alpha, \beta \in \mathbb{R}$，$\alpha f + \beta g \in R(D)$ 且 $\displaystyle\int_D (\alpha f + \beta g) \text{d} \mathbf{v} = \alpha \int_D f \text{d} \mathbf{v} + \beta \int_D g \text{d} \mathbf{v}$。
- (2) 乘积：$fg \in R(D)$。
- (3) 绝对值：$|f| \in R(D)$ 且 $\left| \displaystyle\int_D f \text{d} \mathbf{v} \right| \leq \displaystyle\int_D |f| \text{d} \mathbf{v}$。
- (4) 单调性：若 $f \leq g$，则 $\displaystyle\int_D f \text{d} \mathbf{v} \leq \int_D g \text{d} \mathbf{v}$。
- (5) 区域可加性：设 $D = D_1 \cup \cdots \cup D_m$，各 $D_j$ 可求体积且其内部互不相交，则

$$
\int_D f \text{d} \mathbf{v} = \sum_{j = 1}^m \int_{D_j} f \text{d} \mathbf{v}
$$

- (6) 重积分的第一中值定理：若 $D$ 道路连通，$f \in C(D)$，$g \in R(D)$ 且 $g \geq 0$，则

$$
\exists \boldsymbol{\xi} \in D, \text{s.t. } \int_D f(\mathbf{x}) g(\mathbf{x}) \text{d} \mathbf{v} = f(\boldsymbol{\xi}) \int_D g(\mathbf{x}) \text{d} \mathbf{v}
$$

#### 引理：积分边界可忽略

内容：设 $D$ 为可求体积的有界闭区域，$f \in R(D)$，$g$ 在 $D$ 上有界，若 $f, g$ 在 $\mathring{D}$ 中处处相等，则 $g \in R(D)$，且

$$
\int_D f \text{d} \mathbf{v} = \int_D g \text{d} \mathbf{v}
$$

证明：

- 令 $h = g - f$，设 $M = \displaystyle\sup_D |h|$。由 $D$ 可求体积可知 $V(\partial D) = 0$，而 $D_{\text{dc}}(|h|) \subset \partial D$，故 $D_{\text{dc}}(|h|)$ 零测，进而由 Lebesgue 定理可知 $|h| \in R(D)$。
- 任给 $D$ 的分割 $P = \{\Delta D_j\}_{j = 1}^k$，其中若 $V(\Delta D_j) > 0$，假设 $\Delta D_j \cap \mathring{D} = \varnothing$，则由 $\Delta D_j \subset D$ 可知 $\Delta D_j \subset \partial D$，于是 $V(\Delta D_j) = 0$，导出矛盾！
- 因而 $\Delta D_j \cap \mathring{D} \neq \varnothing$，这意味着相应的 $m_j = 0$，故

$$
\underline{S}_{|h|}(P) = \sum_{j = 1}^k m_j \Delta V_j = \sum_{\Delta V_j > 0} m_j \Delta V_j = 0
$$

- 于是 $\displaystyle\int_D |h| \text{d} \mathbf{v} = \inf_P \underline{S}_{|h|}(P) = 0$，得到 $\displaystyle\int_D h \text{d} \mathbf{v} = 0$，故

$$
\int_D g \text{d} \mathbf{v} = \int_D f \text{d} \mathbf{v} + \int_D h \text{d} \mathbf{v} = \int_D f \text{d} \mathbf{v}
$$

[例 2] 设 $D = [0, 1]^2$，$f(x, y) = \begin{cases} 1, & x \in \left[ 0, \dfrac{1}{2} \right] \\ 0, & x \in \left( \dfrac{1}{2}, 1 \right] \end{cases}$，求证：$f \in R(D)$；并求 $\displaystyle\iint_D f(x, y) \text{d} x \text{d} y$。

解：

- $D_{\text{dc}}(f) = \left\{ \dfrac{1}{2} \right\} \times [0, 1]$ 为零测集，由 Lebesgue 定理可知 $f \in R(D)$。
- 将 $D$ 沿 $x = \dfrac{1}{2}$ 切成两块闭矩形 $D_1 = \left[ 0, \dfrac{1}{2} \right] \times [0, 1], D_2 = \left[ \dfrac{1}{2}, 1 \right] \times [0, 1]$，则由区域可加性可知

$$
\iint_D f(x, y) \text{d} x \text{d} y = \iint_{D_1} f(x, y) \text{d} x \text{d} y + \iint_{D_2} f(x, y) \text{d} x \text{d} y
$$

- 在 $D_1$ 上 $f \equiv 1$，故前者为 $V(D_1) = \dfrac{1}{2}$；在 $D_2$ 上，$f$ 仅在 $x = \dfrac{1}{2}$ 的边界上取 $1$、其余点均取 $0$，故后者为 $0$。总之有

$$
\iint_D f(x, y) \text{d} x \text{d} y = \frac{1}{2}
$$

[例 3]

- 记 $\mathbb{Q} \cap [0, 1] = \{x_k\}_{k \in \mathbb{N}_+}$，设 $D = [0, 1]^2$，定义

$$
f(x, y) = \begin{cases}
\dfrac{1}{k}, &\quad x = x_k, k \in \mathbb{N}_+ \\
0, &\quad \text{otherwise}
\end{cases}
$$

- 求证：$f \in R(D)$ 且 $\displaystyle\iint_D f(x, y) \text{d} \mathbf{v} = 0$。

证明：

- $D_{\text{dc}}(f) = (\mathbb{Q} \cap [0, 1]) \times [0, 1]$ 为零测集，由 Lebesgue 定理可知 $f \in R(D)$。
- $\forall \epsilon > 0$，令 $N = \max \left\{ \left\lfloor \dfrac{2}{\epsilon} \right\rfloor, 2 \right\}$，将 $x_1, \cdots, x_N$ 从小到大排列为 $p_1, \cdots, p_N$（其中 $p_i = x_{k_i}$，$\{k_1, \cdots, k_N\}$ 构成 $\{1, \cdots, N\}$ 的排列），取 $\delta > 0$ 使得

$$
2 \delta \leq \min_{i = 1}^{N - 1} |p_{i + 1} - p_i|, \quad \sum_{i = 1}^N \frac{1}{i} \cdot 2 \delta < \frac{\epsilon}{2}
$$

- 作 $D$ 的分割

$$
\begin{aligned}
P = \ & \{[\max\{p_i - \delta, 0\}, \min\{p_i + \delta, 1\}] \times [0, 1]\}_{i = 1}^N \cup \\
& \{[p_i + \delta, p_{i + 1} - \delta] \times [0, 1]\}_{i = 1}^{N - 1} \cup \\
& \{[0, p_1 - \delta] \times [0, 1] \mid p_1 - \delta > 0\} \cup \{[p_N + \delta, 1] \times [0, 1] \mid p_N + \delta < 1\}
\end{aligned}
$$

- 可见

$$
\underline{S}_f(P) = 0, \quad \bar{S}_f(P) \leq \sum_{i = 1}^N 2 \delta \cdot \frac{1}{k_i} + (1 - (N - 1) \cdot 2 \delta) \cdot \frac{1}{N + 1} < \sum_{i = 1}^N 2 \delta \cdot \frac{1}{i} + \frac{\epsilon}{2} < \epsilon
$$

- 故有 $\displaystyle\iint_D f(x, y) \text{d} \mathbf{v} = 0$。

### 重积分的计算

#### 定理：矩形区域上重积分化累次积分

内容：设 $D = [a, b] \times [c, d]$，$f \in R(D)$。对于固定的 $x \in [a, b]$，记 $\varphi(x), \psi(x)$ 分别为 $f(x, \cdot)$ 在 $[c, d]$ 上的下积分和上积分。则 $\varphi, \psi \in R[a, b]$ 且

$$
\int_a^b \varphi(x) \text{d} x = \int_a^b \psi(x) \text{d} x = \iint_D f(x, y) \text{d} x \text{d} y
$$

证明：

- 对 $D$ 做水平竖直分割 $P_x : a = x_0 < \cdots < x_k = b, P_y : c = y_0 < \cdots y_l = d$，取 $\Delta D_{i, j} = [x_{i - 1}, x_i] \times [y_{j - 1}, y_j]$ 构成 $D$ 的分割 $P = P_x \times P_y \triangleq \{\Delta D_{i, j}\}_{1 \leq i \leq k, 1 \leq j \leq l}$。
- 记 $I = \displaystyle\iint_D f(x, y) \text{d} x \text{d} y$，由 $f \in R(D)$ 可知：$\forall \epsilon > 0$，$\exists \delta > 0$，$\text{s.t. }$ 只要 $\|P_x\|, \|P_y\| < \dfrac{\delta}{\sqrt{2}}$，就有 $\|P\| < \delta$，进而对于任给的取点法 $\boldsymbol{\xi} = \{\boldsymbol{\xi}_{i, j}\}_{1 \leq i \leq k, 1 \leq j \leq l}$ 都有

$$
I - \epsilon < \sum_{i = 1}^k \sum_{j = 1}^l f(\boldsymbol{\xi}_{i, j}) \Delta x_i \Delta y_j < I + \epsilon
$$

- 特别地，令 $\boldsymbol{\xi}_{i, j} = (\xi_i, \eta_{i, j})$，再取上下确界得到

$$
I - \epsilon \leq \sum_{i = 1}^k \sum_{j = 1}^l \inf_{\eta \in [y_{j - 1}, y_j]} f(\xi_i, \eta) \Delta x_i \Delta y_j \leq \sum_{i = 1}^k \sum_{j = 1}^l \sup_{\eta \in [y_{j - 1}, y_j]} f(\xi_i, \eta) \Delta x_i \Delta y_j \leq I + \epsilon
$$

- 请注意 $\displaystyle\sum_{j = 1}^l \inf_{\eta \in [y_{j - 1}, y_j]} f(\xi_i, \eta) \Delta y_j, \sum_{j = 1}^l \sup_{\eta \in [y_{j - 1}, y_j]} f(\xi_i, \eta) \Delta y_j$ 分别为 $f(\xi_i, \cdot)$ 在 $[c, d]$ 上的 Darboux 下和与上和，故有

$$
I - \epsilon \leq \sum_{i = 1}^k \varphi(\xi_i) \Delta x_i \leq \sum_{i = 1}^k \psi(\xi_i) \Delta x_i \leq I + \epsilon
$$

- 由 $\epsilon$ 的任意性即得

$$
\int_a^b \varphi(x) \text{d} x = \int_a^b \psi(x) \text{d} x = I
$$

推论：

- (1) 若对于任意的 $x \in [a, b]$，$I(x) = \displaystyle\int_c^d f(x, y) \text{d} y$ 存在，则

$$
\iint_D f(x, y) \text{d} x \text{d} y = \int_a^b I(x) \text{d} x = \int_a^b \text{d} x \int_c^d f(x, y) \text{d} y
$$

- (2) 若对于任意的 $y \in [c, d]$，$J(y) = \displaystyle\int_a^b f(x, y) \text{d} x$ 存在，则

$$
\iint_D f(x, y) \text{d} x \text{d} y = \int_c^d J(y) \text{d} y = \int_c^d \text{d} y \int_a^b f(x, y) \text{d} x
$$

- (3) 若 $f \in C(D)$，则两个累次积分均存在，且

$$
\iint_D f(x, y) \text{d} x \text{d} y = \int_a^b \text{d} x \int_c^d f(x, y) \text{d} y = \int_c^d \text{d} y \int_a^b f(x, y) \text{d} x
$$

#### 定理：曲边梯形区域上化累次积分

内容：设 $y_1, y_2 \in C[a, b]$，$\forall x \in [a, b], y_1(x) \leq y_2(x)$，则：

- (1) $D = \left\{ (x, y) \in \mathbb{R}^2 \mid y_1(x) \leq y \leq y_2(x), x \in [a, b] \right\}$ 可求体积。
- (2) 若 $f \in R(D)$，且 $\forall x \in [a, b]$，$I(x) = \displaystyle\int_{y_1(x)}^{y_2(x)} f(x, y) \text{d} y$ 存在，则

$$
\iint_D f(x, y) \text{d} x \text{d} y = \int_a^b I(x) \ \text{d} x = \int_a^b \text{d} x \int_{y_1(x)}^{y_2(x)} f(x, y) \text{d} y.
$$

证明：

- (1) 首先有 $\partial D = \Gamma_1 \cup \Gamma_2 \cup (\{a\} \times [y_1(a), y_2(a)]) \cup (\{b\} \times [y_1(b), y_2(b)])$，其中

$$
\Gamma_1 = \{(x, y_1(x)) \mid x \in [a, b]\}, \quad \Gamma_2 = \{(x, y_2(x)) \mid x \in [a, b]\}
$$

- $\forall \epsilon > 0$，由 $y_1$ 的一致连续性可知

$$
\exists \delta \in (0, b - a), \text{s.t. } \forall x_1, x_2 \in [a, b], |x_1 - x_2| < \delta \Rightarrow |y_1(x_1) - y_1(x_2)| < \frac{\epsilon}{4(b - a)}
$$

- 构造 $\Gamma_1$ 的恰当覆盖

$$
\mathcal{A}_{\Gamma_1} = \left\{ [a + (i - 1) \delta, a + i \delta] \times \left[ y_1(a + (i - 1) \delta) - \frac{\epsilon}{4(b - a)}, y_1(a + (i - 1) \delta) + \frac{\epsilon}{4(b - a)} \right] \right\}_{i = 1}^{\lceil \frac{b - a}{\delta} \rceil}
$$

- 算得 $M(\Gamma_1) \leq M(\mathcal{A}_{\Gamma_1}) < \epsilon$。由 $\epsilon$ 的任意性即得 $M(\Gamma_1) = 0$，也即 $V(\Gamma_1) = 0$。
- 同理可知 $V(\Gamma_2) = 0$，而后两者的体积显然也为零，故 $V(\partial D) = 0$，于是 $D$ 可求体积。
- (2) 令 $c = \displaystyle\min_{x \in [a, b]} y_1(x), d = \max_{x \in [a, b]} y_2(x)$，定义 $f_A$ 是 $f$ 到 $U = [a, b] \times [c, d]$ 的零延拓，则 $D_{\text{dc}}(f_A) \subset D_{\text{dc}}(f) \cup \partial D$。
- 前者由 Lebesgue 定理可知其零测、后者由 $D$ 可求体积即得 $V(\partial D) = 0$，于是 $D_{\text{dc}}(f_A)$ 为零测集，故 $f_A \in R(U)$，于是

$$
\iint_D f(x, y) \text{d} x \text{d} y = \iint_U f_A(x, y) \text{d} x \text{d} y = \int_a^b \text{d} x \int_c^d f_A(x, y) \text{d} y = \int_a^b \text{d} x \int_{y_1(x)}^{y_2(x)} f(x, y) \text{d} y
$$

- 其中第一个等号源自 $U = D \cup V_1 \cup V_2$，其中

$$
V_1 = \{(x, y) \mid a \leq x \leq b, c \leq y \leq y_1(x)\}, \quad V_2 = \{(x, y) \mid a \leq x \leq b, y_2(x) \leq y \leq d\}
$$

- 同 (1) 理可知其可求体积，且只在边界非零。

曲边梯形的底平行于 $y$ 轴的情形的表述与证明同理，略去。
